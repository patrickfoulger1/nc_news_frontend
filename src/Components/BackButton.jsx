import ArrowLeft from "@phosphor-icons/react";

function BackButton(article_id) {
  return (
    <button className="">
      <ArrowLeft size={32} color="#f0f0f0" weight="fill" />
    </button>
  );
}

export default BackButton;
